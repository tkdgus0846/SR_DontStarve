#pragma once
class CCalculator
{
private:
	CCalculator();
	~CCalculator();

public:
	/*static _float				Compute_HeightOnTerrain(const _vec3* pPos,
		const _vec3* pTerrainVtxPos,
		const _ulong& dwCntX,
		const _ulong& dwCntZ,
		const _ulong& dwVtxItv = 1);

	static _vec3				Picking_OnTerrain(HWND hWnd, LPDIRECT3DDEVICE9 pGraphicDev, class CTerrainTex* pTerrainBufferCom, class CTransform* pTerrainTransCom);*/
};

